[project]
name = "fivetran-snowflake-dbt-demo"
version = "0.1.0"
description = "ELT demo: Neon Postgres, Fivetran, Snowflake, and dbt Core"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "dbt-snowflake>=1.9,<2",
]
