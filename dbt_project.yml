# SleekMart OMS transforms (from sleekdata/oms_fivetran_transforms).
# Reads the Fivetran replica in Snowflake, not Neon.

name: 'oms_dbt_proj'
version: '1.0.0'
config-version: 2

profile: 'oms_dbt_proj'

vars:
  # Fivetran prefixes the destination schema with the connection name unless you
  # pick source naming, and the prefix cannot be changed after the connection is
  # created. This connection is named postgres_demo, so Neon's l1_landing lands
  # as POSTGRES_DEMO_L1_LANDING. Confirm yours with
  #   SHOW SCHEMAS IN DATABASE FIVETRAN_DEMO;
  fivetran_database: FIVETRAN_DEMO
  fivetran_schema: POSTGRES_DEMO_L1_LANDING

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# In dbt, the default materialization for a model is a view. This means, when you run 
# dbt run or dbt build, all of your models will be built as a view in your data platform. 
# The configuration below will override this setting for models in the example folder to 
# instead be materialized as tables. Any models you add to the root of the models folder will 
# continue to be built as views. These settings can be overridden in the individual model files
# using the `{{ config(...) }}` macro.

models:
  oms_dbt_proj:

    # Customer project
    customer_rev:
      +schema: "{{ 'TRANSFORM' }}"  
      customerrevenue:
        +schema: "{{ 'SERVE' }}"   
        +tags: ['daily']  # runs daily

    # Employee reporting project
    emp_reporting:
      +schema: "{{ 'TRANSFORM' }}"  
      emp_weekly_sales:
        +schema: "{{ 'SERVE' }}"   
        +tags: ['weekly']  # runs weekly